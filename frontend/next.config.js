/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['example.com'], // Update with actual image sources
  },
};

module.exports = nextConfig;
